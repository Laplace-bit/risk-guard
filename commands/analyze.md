---
description: "Run the full risk engine on a structured case file"
---

Run the full risk engine on a structured case JSON file.

Reads the JSON case file, runs the risk engine, and returns a detailed score breakdown with compound rule analysis.

Example: `/risk-guard analyze path/to/case.json`
